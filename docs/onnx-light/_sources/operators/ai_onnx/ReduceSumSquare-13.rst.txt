.. _op_ai_onnx_ReduceSumSquare-13:

ReduceSumSquare - version 13
============================

This page documents version **13** of operator **ReduceSumSquare**. See :doc:`ReduceSumSquare` for the latest version (since version 18).

- **Domain**: ``ai.onnx``
- **Since version**: 13

Computes the sum square of the input tensor's elements along the provided axes.
The resulting tensor has the same rank as the input if keepdims equals 1.
If keepdims equals 0, then the resulting tensor has the reduced dimension pruned.
The axes attribute specifies which dimensions to reduce. Negative axes are supported.
If axes is not provided, all dimensions are reduced.
Reduction over an empty set of values yields 0.

**Inputs**

- **data** (*T*): An input tensor.

**Outputs**

- **reduced** (*T*): Reduced output tensor.

**Attributes**

- **axes** (*int[]*): A list of integers, along which to reduce. The default is to reduce over all the dimensions of the input tensor. Accepted range is [-r, r-1] where r = rank(data).
- **keepdims** (*int*): Keep the reduced dimension or not, default 1 means keep reduced dimension.

**Type Constraints**

- **T**: Constrain input and output types to high-precision numeric tensors.
  Allowed types: tensor(bfloat16), tensor(double), tensor(float), tensor(float16), tensor(int32), tensor(int64), tensor(uint32), tensor(uint64).

Differences with previous version (11)
--------------------------------------

**SchemaDiff**: ``ReduceSumSquare`` (domain ``'ai.onnx'``)

* old version: 11
* new version: 13
* breaking: no

**Type constraints:**

* changed 'T': added types: ['tensor(bfloat16)']

**Documentation:**

* line similarity: 0.20 (+5/-3 lines)

.. code-block:: diff

    --- ReduceSumSquare v11
    +++ ReduceSumSquare v13
    @@ -1,4 +1,6 @@
    -Computes the sum square of the input tensor's element along the provided axes.
    +Computes the sum square of the input tensor's elements along the provided axes.
     The resulting tensor has the same rank as the input if keepdims equals 1.
    -If keepdims equal 0, then the resulted tensor have the reduced dimension pruned.
    -Negative axes are supported in the axes attribute.
    +If keepdims equals 0, then the resulting tensor has the reduced dimension pruned.
    +The axes attribute specifies which dimensions to reduce. Negative axes are supported.
    +If axes is not provided, all dimensions are reduced.
    +Reduction over an empty set of values yields 0.
