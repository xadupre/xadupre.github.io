name: DATA onnx-light inplace metadata coverage

# Weekly job that runs the backend tests tagged ``inplace`` bundled with
# ``onnx-light`` and compares the metadata produced by the current
# in-place-reuse algorithm against the metadata embedded in those tests.
# The resulting JSON cache is written to
# ``cache_data/onnx-light/inplace_reuse_coverage.json`` and consumed by
# ``dashboard/onnx-light/inplace-reuse-coverage.html``.

on:
  schedule:
    - cron: "53 7 * * 1"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  record:
    if: github.event_name == 'schedule' || github.actor == github.repository_owner || github.actor == 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout xadupre.github.io
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ secrets.BOT_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Checkout cache data
        uses: actions/checkout@v6
        with:
          repository: xadupre/cache_data
          path: cache_data
          ref: main
          fetch-depth: 0
          ssh-key: ${{ secrets.CACHE_DATA_SSH_KEY }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Checkout onnx-light
        uses: actions/checkout@v6
        with:
          repository: xadupre/onnx-light
          path: onnx-light

      - name: Detect CPU cores
        run: |
          cores="$(nproc)"
          echo "Detected ${cores} CPU cores."
          echo "CMAKE_BUILD_PARALLEL_LEVEL=${cores}" >> "$GITHUB_ENV"

      - name: Install onnx and onnx-light
        run: |
          python -m pip install --upgrade pip
          pip install onnx numpy scikit-build-core nanobind
          pip install -e ./onnx-light -v

      - name: pip freeze
        run: pip freeze

      - name: Record inplace metadata coverage snapshot
        run: |
          python -u scripts/record_onnx_inplace_reuse_coverage.py

      - name: Commit and push cache data
        run: |
          bash scripts/commit_cache_data.sh cache_data "Update onnx-light inplace metadata coverage cache"
